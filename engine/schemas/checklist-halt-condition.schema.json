{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "checklist-halt-condition",
  "title": "Checklist Item with Halt Condition",
  "description": "Defines a checklist item that can optionally halt pipeline execution on failure",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "description": "Unique identifier for this check"
    },
    "description": {
      "type": "string",
      "description": "Human-readable description of what is being checked"
    },
    "halt_on_fail": {
      "type": "boolean",
      "default": false,
      "description": "If true, stops checklist evaluation when this item fails"
    },
    "check_type": {
      "type": "string",
      "enum": ["file_exists", "test_passes", "grep_match", "custom"],
      "description": "Type of check to perform"
    },
    "check_value": {
      "type": "string",
      "description": "Value for the check: file path, command, file:pattern, or expression"
    },
    "severity": {
      "type": "string",
      "enum": ["info", "warning", "error", "critical"],
      "default": "error",
      "description": "Severity level of a failure"
    }
  },
  "required": ["id", "description", "check_type", "check_value"],
  "additionalProperties": false
}
