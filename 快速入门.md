# 快速入门

> 首次使用 Claude Code FPGA Harness？从这里开始。< 5 分钟上手。

---

## 1. 三分钟快速开始

```powershell
# 0. 换机 / 新克隆：把入库的声明物化成本机文件（只需跑一次）
#    settings.json 与 .claude/workflows/ 都在 .gitignore 里，全新 checkout 上是空的，
#    不跑这步后面的诊断会报 hook 缺失、第 3 节的工作流也起不来。
node engine/scripts/render-hook-settings.cjs   # engine/hooks/registrations.json → settings.json 的 hooks
node engine/scripts/sync-workflow-mirror.cjs   # workflows/ → .claude/workflows/ 镜像

# 1. 运行诊断检查环境
node engine/diagnostics.cjs

# 2. 检测 EDA 工具链
node engine/scripts/eda-detect.cjs

# 3. 创建你的第一个项目
node engine/scripts/harness-init.cjs --project my_project

# 4. 验证工具链
cd my_project

# 用 node 脚本检查可用工具
node "%USERPROFILE%\.claude\engine\scripts\eda-detect.cjs"

# 直接调用 EDA 工具做语法检查 (选一个检测到的):
#   vlog -lint 01_src\00_hdl\my_project_top\my_project_top.sv        (ModelSim/Questa)
#   xvlog -sv 01_src\00_hdl\my_project_top\my_project_top.sv         (Vivado, 无 -lint 选项)
#   iverilog -g2012 -o NUL 01_src\00_hdl\my_project_top\my_project_top.sv (Icarus)
```

> **关于 `make`**: 本 harness 生成的 Makefile 需要 `make` 工具。
> Windows 用户可通过 **Git Bash** (随 Git for Windows 安装)、Cygwin 或 MSYS2 获得 `make`。
> 如果不想安装 `make`，所有操作都可用 `node` 脚本或直接调用 EDA 工具代替。

> **预期结果**：
> - 诊断脚本应报告 ✅ 全部通过
> - EDA 检测应显示至少一个可用工具（如 iverilog）
> - EDA 工具 `-lint` 命令应无报错

---

## 2. 五层架构总览

```
┌──────────────────────────────────────────────────────────────────┐
│  L5 技能层 (Skills)                                              │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌─────────┐ ┌──────────┐ │
│  │ hdl- │ │ tdd  │ │ debug│ │ code-│ │ rag-    │ │ /start   │ │
│  │coding│ │      │ │     │ │review│ │skill    │ │ /handoff │ │
│  └──┬───┘ └──────┘ └──────┘ └──┬───┘ └────┬────┘ └────┬─────┘ │
│     │                          │          │           │        │
│     └──────────┬───────────────┘          │           │        │
│                ▼                          ▼           ▼        │
│         DAG 工作流 (9-Phase)        知识库检索      会话交接    │
├──────────────────────────────────────────────────────────────────┤
│  L4 认知层 (Cognition)                                           │
│  挫败检测 → 自动切换 7 种推理模式 (RCA/第一性原理/减法/倒推/…)  │
├──────────────────────────────────────────────────────────────────┤
│  L3 交接层 (Handoff)                                             │
│  /start → 任务协议 (var/active-task.yaml) → /handoff → 日志归档  │
├──────────────────────────────────────────────────────────────────┤
│  L2 记忆/知识层 (Memory & Knowledge)                             │
│  ┌──────────────────┐  ┌──────────────────┐  ┌────────────────┐ │
│  │ SQLite FTS5      │  │ knowledge/       │  │ memory/        │ │
│  │ 记忆/事件/技能/  │  │ 领域知识 (FPGA/  │  │ 经验/错误/项目 │ │
│  │ 成本追踪         │  │ Comm/MATLAB/Py)  │  │ 永久学习       │ │
│  └──────────────────┘  └──────────────────┘  └────────────────┘ │
│         ▲                     ▲                     ▲            │
│         └─────────────┬───────┴──────┬──────────────┘            │
│                       ▼              ▼                           │
│                  检索工具链: SQLite → Grep → git → rag-skill    │
├──────────────────────────────────────────────────────────────────┤
│  横切: 闭环(loop) · 代码图(graph) · 工程资产库                   │
│  loop  → Stop 钩子把"没收敛"顶回去继续干 (显式开启, 默认零开销)  │
│  graph → 代码图 + 跨域边, 出改动影响面与模块验证顺序             │
│  assets→ 需求→文档→Golden→CBB 反偏离锚链, 防设计跑偏且无人报错   │
├──────────────────────────────────────────────────────────────────┤
│  L1 边界层 (Hooks)                                               │
│  15 条注册 × 8 事件 — 物理拦截, LLM 不可绕过 (热路径由 router 合并)│
│  ┌────────┐ ┌────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐  │
│  │ lint   │ │ file   │ │ 挫败     │ │ signal   │ │ cost     │  │
│  │ 门禁   │ │ 保护   │ │ 检测     │ │ 采集     │ │ 追踪     │  │
│  └────────┘ └────────┘ └──────────┘ └──────────┘ └──────────┘  │
├──────────────────────────────────────────────────────────────────┤
│  L0 核心规则 (Core Rules)                                       │
│  四条铁律 + Lint First + 验证闭环                                │
└──────────────────────────────────────────────────────────────────┘
```

### 9-Phase HDL 工作流 (DAG)

```
Phase 0: 基础设施 ───→ Phase 1: 架构 ──┬─→ Phase 2: 定点量化 ──┐
                                        │                        │
                                        └─→ Phase 3: TB+向量 ───┤
                                                                ▼
                                        Phase 4: 逐模块 RTL + MATLAB 验证 [MUST]
                                                                │
                                                                ▼
                                        Phase 5: 顶层集成 + 全链仿真 [MUST]
                                                               / \
                                                              /   \
                                        Phase 6: 回归覆盖率  Phase 7: 代码审查
                                                              \   /
                                                               \ /
                                                                ▼
                                        Phase 8: 报告 + Verifier (终验)
```

---

## 3. 我的第一个 HDL 模块

**目标**: 用工作流走完一个 `fir_filter` 模块。

```bash
# 一句话启动 10-Phase DAG 工作流 (v3.4 证据驱动)
# 在工作流对话中输入:
Workflow({name: 'hdl-coding-dag-workflow', args: {
  modules: ['fir_filter']
}})
```

工作流会按顺序执行以下阶段（每个阶段自动出结果后进入下一阶段）：

| Phase | 做什么 | 产出 |
|:------|:-------|:-----|
| 0 | 检查 EDA 工具 + 编译环境 | lint 通过 (直接调用 EDA 工具) |
| 1 | 设计架构 + 写 algorithm_spec.md | 框图 + architecture.yaml |
| 2 | MATLAB 定点量化 + 位宽扫描 | fixed_point_report |
| 3 | 自检 TB + MATLAB golden 向量 | TB 编译通过 |
| 4 | **[MUST]** 写 RTL → 生成 check_<module>.py → bash 运行 | JSON 证据文件 (02_sim/check_results/*.json) |
| 4.5 | **[证据门禁]** 独立读 JSON 证据，高安全模块追加对抗验证 | 文件检查全 PASS |
| 5 | **[MUST]** 顶层集成 → 全链仿真 (入口依赖 Phase 4.5) | 全链 PASS |
| 6 | 真跑回归 + 覆盖率 | `make regress` 全绿 (需 Git Bash/Cygwin) |
| 7 | 代码审查 | 审查报告 PASS |
| 8 | 最终报告 + Verifier 终验 (含证据交叉检查) | 报告归档 |

**小改动不走全流程？** 加 `lite: true`：

```bash
Workflow({name: 'hdl-coding-dag-workflow', args: {
  modules: ['fir_filter'],
  lite: true    # 跳过 Phase 2 + Phase 6, Phase 4.5 文件检查保留
}})
```

---

## 3.5 三个横切能力：什么时候该用

前面三节讲的是"怎么跑一个模块"。下面三样是跨任务的，**不用也能干活，但用对了省很多返工**。

### 闭环 loop —— 让"没修好"自动顶回去

**什么时候用**：跨多轮才能收敛的活（回归修到全绿、门禁修到 certified），
且收敛条件**能被脚本读出来**。一次就能做完的改动不要用。

```bash
# 开：目标 + 判据(声明式 JSON) + 迭代预算
node engine/scripts/loop-ctl.cjs start --goal "回归修到全绿" \
  --criteria '[{"type":"command","run":"node engine/scripts/test-hooks/run-all-tests.cjs","expectExit":0}]' \
  --budget 6

node engine/scripts/loop-ctl.cjs status    # 看迭代史
node engine/scripts/loop-ctl.cjs check     # 只求值判据，不进循环
node engine/scripts/loop-ctl.cjs abandon --reason "改用人工排查"
```

三条要先知道的规矩：

- **不开就完全没影响** —— 没 `start` 过的会话 Stop 直接放行；
- **判据读不出结论 = 未收敛**，不是"通过"；空判据也判未收敛；
- **预算耗尽时打印"未收敛"**，不会因为跑满次数就说成功。

### 代码图 graph —— 改之前先看波及谁

索引由 hook 自动维护（SessionStart 全量增量 + 每次 Edit/Write 单文件），
你只需要查：

```bash
node engine/scripts/module-order.cjs --impacted cdc_sync      # 改它会波及哪些模块
node engine/scripts/module-order.cjs --modules a,b,c          # 推导验证顺序
node engine/scripts/cg-queries.cjs callers <projectId> <符号>  # 谁调用/例化了它
```

⚠️ 图**只能**给出层次序（子模块先于父模块验证）。同层兄弟模块之间的数据流先后
它推不出来，会保留你给的相对顺序 —— 不要以为它替你排好了全序。
索引陈旧时查询返回空结果而不是旧图。

### 工程资产库 —— 别从零写已经认证过的模块

```bash
cd engineering-assets
node tools/catalog-gen.cjs                      # 看 catalog/CATALOG.md 挑资产
cat cbb/rrc_polyphase_fir/docs/limitations.md   # 【必读】签字时接受的边界
```

**先读限制清单再决定用不用。** 把 "certified" 当成"随便用"是最典型的误用 ——
每个包都明确列了器件口径、未取证项和接口偏离。

---

## 4. 常见任务快速入口

| 我想做… | 怎么做 |
|:--------|:-------|
| 开始新 session | 输入 `/start` 或「开始工作」 |
| 新建 FPGA 项目 | `node engine/scripts/harness-init.cjs` |
| 写 RTL 模块 | 输入 `/hdl-coding` 或启动 DAG 工作流 |
| 快速生成代码模板 | 输入 `/rtl-gen` |
| 审查代码 | 输入 `/code-review` 或启动 review 工作流 |
| 查知识库 (FPGA/通信/MATLAB) | 输入 `/rag-skill` 或「查一下 XXX」 |
| 搜代码 | 输入 `/code-search` 或「搜 XXX」 |
| Git 操作 | 输入 `/git-expert` 或「提交/推送」 |
| 调试问题 | 输入 `/debugging` 或「为什么会错」 |
| 生成文档 | 输入 `/doc-gen` 或「生成文档」 |
| 测试驱动开发 | 输入 `/tdd` 或「先写测试」 |
| 查项目经验教训 | 输入「我记得某个决策…」→ 自动触发 SQLite 检索 |
| 结束今天的工作 | 输入 `/handoff` 或「收工/保存进度」 |
| 做小改动（Lite 模式） | `Workflow({...args, lite: true})` |
| 检查 EDA 工具 | `node engine/scripts/eda-detect.cjs` |
| 检查波形 | `node engine/scripts/fpga-wave-helper.cjs check dump.vcd` |
| 检查 ModelSim 兼容性 | `node engine/scripts/modelsim-sv-constraints.cjs 01_src/` |
| 让"没修好"自动继续 | `node engine/scripts/loop-ctl.cjs start --goal … --criteria …` |
| 改前查影响面 | `node engine/scripts/module-order.cjs --impacted <模块>` |
| 找现成的认证模块 | `engineering-assets/catalog/CATALOG.md` |
| 模块回灌进资产库 | `node engineering-assets/tools/gate-runner.cjs <包目录> --repo-root ..` |
| 语法检查 (无 `make`) | `vlog -lint 01_src\00_hdl\my_module\*.sv` 或检测到的 EDA 工具 |
| 运行仿真 (无 `make`) | `vsim -c -do "run -all; quit" work.top_module` |

---

## 5. 快速排障

| 问题 | 原因/解决 |
|:-----|:----------|
| `EDA 工具未检测到` | 装个开源版: `iverilog` 或 `verilator` (安装到 D 盘) |
| 找不到 `make` 命令 | Windows 需安装 Git Bash/Cygwin/MSYS2 才能用 `make`；或用 EDA 工具直接调用: `vlog -lint <file>` |
| `node engine/diagnostics.cjs` 报 hook 失败 | 换机/新克隆没跑第 0 步 — `node engine/scripts/render-hook-settings.cjs` 重建 `settings.json`；已有环境用 `--check` 看是否与模板漂移。**hook 只放 `settings.json`**，`settings.local.json` 自 2026-07-27 起不再承载 hook |
| `Workflow({name: …})` 找不到工作流 | `.claude/workflows/` 镜像未重建 — `node engine/scripts/sync-workflow-mirror.cjs`（`--check` 可校验漂移） |
| 工作流卡在 Phase 4 | 模块验证不通过 → 查看验证矩阵找到 ❌ 模块 → 修复 RTL → 重仿 |
| Phase 5 拒绝执行 | Phase 4 验证矩阵有 ❌ — 全部通过后才能集成 |
| 工作流太慢 | 加 `lite: true` 跳过 P2+P6 |
| Stop 时被顶回去继续干 | 有 active 循环未收敛 — `loop-ctl status` 看差哪条判据；不想继续用 `loop-ctl abandon --reason …` |
| 图查询返回空 | 索引陈旧或没建 — 结果里会带 `staleIndex`；重开一个 session 触发 SessionStart 同步 |
| 门禁说 blocked 而不是 fail | 该门未接线或缺前置条件（如上板），**不是静默放行也不是待办** — 看 `integration/registry.json` 的 `unblock_requires` |

---

## 6. 下一步

- 详读 `README.md` — 完整参考手册
- 熟悉 `engineering-assets/knowledge/SCENE_CARDS.md` — 按场景引导知识检索
- 浏览 `skills/` — 全部 30+ 技能定义
- 查看 `memory/projects/` — 项目级记忆与约定
