{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "readmeInput",
  "description": "Input schema for Use when creating, updating, or generating README and documentation files for projects and libraries",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "target": {
      "type": "string",
      "description": "Target file or path for the skill to operate on"
    },
    "options": {
      "type": "object",
      "description": "Additional options for skill execution",
      "additionalProperties": true
    }
  }
}
