{
  "mcpServers": {
    "mcp-pdf": {
      "command": "npx",
      "args": ["-y", "mcp-pdf"]
    },
    "codegraph": {
      "command": "node",
      "args": ["engine/mcp/codegraph-server.cjs"],
      "env": {
        "CODEGRAPH_DB": "${HOME}/.claude/.wright/memory.db"
      }
    }
  }
}
